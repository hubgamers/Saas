import type { TournamentPhaseRepository } from "../domain/tournament-phase.repository";

export const prismaTournamentPhaseRepository: TournamentPhaseRepository = {
  findMany() {
    throw new Error("Implement prismaTournamentPhaseRepository.findMany after adding the Prisma model.");
  },

  create() {
    throw new Error("Implement prismaTournamentPhaseRepository.create after adding the Prisma model.");
  },
};
